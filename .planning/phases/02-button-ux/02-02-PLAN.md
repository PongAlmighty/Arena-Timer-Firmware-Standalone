---
phase: "02-button-ux"
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - include/TimerDisplay.h
  - src/TimerDisplay.cpp
  - src/main.cpp
autonomous: true
requirements:
  - BTN-04
  - BTN-05
  - BTN-06
  - BTN-07

must_haves:
  truths:
    - "TimerDisplay has a setArmed(bool) public method that stores the armed flag and manages blink state on transitions"
    - "When armed, TimerDisplay::update() short-circuits normal rendering and blinks yellow STOP? at 1Hz (500ms on/500ms off)"
    - "When armed, the STOP? text uses color565(255, 200, 0) (warm yellow), default NULL font, size 1, centered at x=17 y=12 on the 64x32 matrix"
    - "On arm transition (false->true), _blink_state=true and _last_blink_ms=millis() are reset for a clean first blink cycle"
    - "On disarm transition (true->false), _blink_state=true is reset so normal rendering resumes visibly immediately"
    - "main.cpp loop() calls timerDisplay.setArmed(buttonHandler.isArmed()) immediately after buttonHandler.poll()"
    - "main.cpp routes stopResetPressed() to wsClient->emitTimerControl(stop) then emitTimerControl(reset) per D-06"
    - "main.cpp routes startStopPressed() to emitTimerControl(stop) when timer isRunning(), emitTimerControl(start) otherwise per D-08"
    - "All wsClient->emitTimerControl() calls in loop() are wrapped with if (wsClient) null-guard"
    - "timerDisplay.update() is called after setArmed() and after all button event routing per D-11 anti-pattern"
    - "pio run completes with 0 errors"
  artifacts:
    - path: "include/TimerDisplay.h"
      provides: "setArmed(bool) declaration and _armed private member"
      contains: "void setArmed(bool armed)"
    - path: "src/TimerDisplay.cpp"
      provides: "setArmed() implementation + armed short-circuit in update()"
      contains: "color565(255, 200, 0)"
    - path: "src/main.cpp"
      provides: "Button event routing and armed display wiring in loop()"
      contains: "emitTimerControl"
  key_links:
    - from: "src/main.cpp loop()"
      to: "timerDisplay.setArmed()"
      via: "buttonHandler.isArmed() return value"
      pattern: "setArmed\\(buttonHandler\\.isArmed\\(\\)\\)"
    - from: "src/TimerDisplay.cpp update()"
      to: "STOP? rendering block"
      via: "_armed early-return guard"
      pattern: "if \\(_armed\\)"
    - from: "src/main.cpp loop()"
      to: "wsClient->emitTimerControl()"
      via: "startStopPressed() / stopResetPressed() one-shot flags"
      pattern: "emitTimerControl\\("
---

<objective>
Wire the Phase 2 button UX end-to-end: add the armed display overlay to TimerDisplay and route button events in main.cpp through emitTimerControl() (D-09 through D-11, D-06, D-08, BTN-07).

TimerDisplay gains `setArmed(bool)` and an armed short-circuit block at the top of `update()` that renders yellow "STOP?" at 1Hz. main.cpp loop() is updated to call `setArmed()` each iteration and dispatch `startStopPressed()` / `stopResetPressed()` through `wsClient->emitTimerControl()`.

Purpose: Completes the full button UX. All four BTN-04/05/06/07 behaviors are active after this plan. The project is in a shippable state for Phase 2.

Output: Modified TimerDisplay.h/cpp and main.cpp. Full `pio run` must pass.
</objective>

<execution_context>
@/Users/themightypong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/themightypong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-button-ux/02-CONTEXT.md
@.planning/phases/02-button-ux/02-RESEARCH.md
@.planning/phases/02-button-ux/02-PATTERNS.md
@.planning/phases/02-button-ux/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TimerDisplay armed overlay — setArmed() + update() short-circuit (D-09, D-10, D-11, BTN-07)</name>
  <files>include/TimerDisplay.h, src/TimerDisplay.cpp</files>
  <read_first>
    - include/TimerDisplay.h — current class declaration, specifically the public method block around update() (lines ~103–107) and the private blink members block (lines ~136–138); look for existing _last_blink_ms, _blink_state, _was_expired
    - src/TimerDisplay.cpp — constructor initializer list (add _armed(false) at end), getTimer() method location (setArmed() goes after it, before update()), update() opening lines (armed short-circuit inserts after `unsigned long current_ms = millis();`), the expired-blink pattern and showMessage() fill+cursor+print+show pattern to reuse
    - .planning/phases/02-button-ux/02-PATTERNS.md §TimerDisplay.h and §TimerDisplay.cpp — exact insertion points, setArmed() implementation with blink reset logic, armed overlay block with x/y cursor values
    - .planning/phases/02-button-ux/02-RESEARCH.md §Pitfall 3 — why blink state must reset on both arm and disarm transitions
  </read_first>
  <action>
    Modify include/TimerDisplay.h:
    - Before the `/// @brief Update and draw the timer` declaration, add the setArmed declaration with doxygen comment:
      `/// @brief Set armed overlay mode. When true, displays yellow "STOP?" blink at 1Hz.`
      `/// Call each loop() with buttonHandler.isArmed() before update().`
      `void setArmed(bool armed);`
    - In the private section, after `bool _was_expired;`, add:
      `bool _armed; // Armed overlay flag — set by main.cpp each loop via setArmed()`

    Modify src/TimerDisplay.cpp:
    - Constructor initializer list: append `, _armed(false)` at the end of the existing list (after `_was_expired(false)`), before the `{` that opens the constructor body. The full list ends: `_was_expired(false), _armed(false) {`.
    - Add `setArmed()` implementation after the `getTimer()` function and before `update()`. Implement the transition-aware setter per D-11 and Pitfall 3:
      - On transition false→true (armed && !_armed): set `_blink_state = true` and `_last_blink_ms = millis()` — clean first blink cycle
      - On transition true→false (!armed && _armed): set `_blink_state = true` — normal rendering starts visible immediately
      - Then set `_armed = armed;`
    - In `update()`, immediately after `unsigned long current_ms = millis();`, add the armed short-circuit block per D-10:
      - `if (_armed) {` opens the block
      - Blink toggle: `if (current_ms - _last_blink_ms >= 500) { _blink_state = !_blink_state; _last_blink_ms = current_ms; }`
      - `_matrix.fillScreen(0);`
      - `if (_blink_state) {` renders text:
        - `_matrix.setFont(NULL);` — force default 5x7 font (same as showMessage() pattern; prevents GFX font sizing issues per Open Question 1)
        - `_matrix.setTextSize(1);`
        - `_matrix.setTextColor(_matrix.color565(255, 200, 0));` — warm yellow per BTN-07 (255, 200, 0 distinguishes from white timer text and red/green threshold colors)
        - `_matrix.setCursor(17, 12);` — x=17 centers 30px ("STOP?" at 5x6px/char) on 64px width; y=12 centers 7px glyph on 32px height
        - `_matrix.print("STOP?");`
      - `}`  (closes if _blink_state)
      - `_matrix.show();`
      - `return;` — skip all existing expired/paused/running logic below
      - `}` (closes if _armed)
  </action>
  <verify>
    <automated>cd /Users/themightypong/Documents/PythonCode/Arena-Timer-Firmware-Standalone && pio run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - include/TimerDisplay.h public section contains `void setArmed(bool armed);`
    - include/TimerDisplay.h private section contains `bool _armed;`
    - src/TimerDisplay.cpp constructor initializer list contains `_armed(false)`
    - src/TimerDisplay.cpp contains `void TimerDisplay::setArmed(bool armed)`
    - src/TimerDisplay.cpp setArmed() body contains `_blink_state = true` in both transition branches (arm and disarm)
    - src/TimerDisplay.cpp update() contains `if (_armed) {` block before the existing expired/blink logic
    - src/TimerDisplay.cpp update() armed block contains `color565(255, 200, 0)`
    - src/TimerDisplay.cpp update() armed block contains `_matrix.setCursor(17, 12)`
    - src/TimerDisplay.cpp update() armed block contains `_matrix.print("STOP?")`
    - src/TimerDisplay.cpp update() armed block ends with `return;`
    - `pio run` exits 0
  </acceptance_criteria>
  <done>TimerDisplay shows yellow blinking "STOP?" when armed, returns to normal timer display on disarm. Blink state is clean on both transitions. Project builds.</done>
</task>

<task type="auto">
  <name>Task 2: main.cpp button event routing (D-06, D-08, BTN-04, BTN-05, BTN-06)</name>
  <files>src/main.cpp</files>
  <read_first>
    - src/main.cpp — current loop() body (lines ~130–139): shows existing call order (buttonHandler.poll(), timerDisplay.update(), Ethernet.maintain(), WebServer::handleClient(), wsClient->poll()); note wsClient null-guard pattern
    - include/Timer.h — isRunning(), isIdle(), isPaused() signatures; confirm isRunning() is the correct branch for "stop" (Pitfall 5: else branch covers both idle and paused → "start")
    - .planning/phases/02-button-ux/02-PATTERNS.md §main.cpp — full loop() replacement showing the exact new call order and nested null-guard structure
    - .planning/phases/02-button-ux/02-RESEARCH.md §Pattern 4 — main.cpp button event routing and toggle logic
  </read_first>
  <action>
    Replace the loop() body in src/main.cpp with the Phase 2 wiring (per D-06, D-08). The new call order is mandatory — do not reorder:

    1. `buttonHandler.poll();` — unchanged, stays first
    2. `timerDisplay.setArmed(buttonHandler.isArmed());` — IMMEDIATELY after poll() per D-09; must come before update()
    3. Chord reset block (stopResetPressed check BEFORE startStopPressed — stopReset takes priority per BTN-06):
       ```
       if (buttonHandler.stopResetPressed()) {
         if (wsClient) {
           wsClient->emitTimerControl("stop");
           wsClient->emitTimerControl("reset");
         }
       }
       ```
       Two sequential calls: "stop" then "reset" — pre-wires Phase 3 two-event sequence; `_timer->stop()` is a no-op when already stopped so this is safe per the resolved Open Question 2 in RESEARCH.md.
    4. Start/stop toggle block (per D-08 — toggle logic lives here, reads timer state):
       ```
       if (buttonHandler.startStopPressed()) {
         if (wsClient) {
           if (timerDisplay.getTimer().isRunning()) {
             wsClient->emitTimerControl("stop");
           } else {
             wsClient->emitTimerControl("start");
           }
         }
       }
       ```
       Toggle condition: `isRunning()` → "stop", else (idle OR paused) → "start" (per Pitfall 5 — else covers both first-press and resume).
    5. `timerDisplay.update();` — after setArmed() and after all button routing
    6. `Ethernet.maintain();` — unchanged
    7. `WebServer::handleClient(timerDisplay);` — unchanged
    8. `if (wsClient) { wsClient->poll(); }` — unchanged

    All `wsClient->emitTimerControl()` calls are inside `if (wsClient)` guards matching the existing null-guard pattern in the file.
  </action>
  <verify>
    <automated>cd /Users/themightypong/Documents/PythonCode/Arena-Timer-Firmware-Standalone && pio run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/main.cpp loop() contains `timerDisplay.setArmed(buttonHandler.isArmed());`
    - src/main.cpp loop() contains `buttonHandler.stopResetPressed()` check
    - src/main.cpp loop() contains `buttonHandler.startStopPressed()` check
    - src/main.cpp loop() contains `wsClient->emitTimerControl("stop")` and `wsClient->emitTimerControl("reset")` inside the stopResetPressed branch
    - src/main.cpp loop() contains `wsClient->emitTimerControl("start")` and `wsClient->emitTimerControl("stop")` inside the startStopPressed branch
    - src/main.cpp loop() contains `timerDisplay.getTimer().isRunning()` as the toggle condition
    - src/main.cpp loop() has `timerDisplay.update()` AFTER `timerDisplay.setArmed(...)` (not before)
    - All `wsClient->emitTimerControl()` calls are inside `if (wsClient) {` blocks
    - `pio run` exits 0
    - <human-check>Hold Button 2 for >= 400ms — matrix displays yellow "STOP?" blinking. Release Button 2 — matrix returns to normal timer display with no state change. Then hold Button 2 again, press Button 1 while holding — timer resets. Separately, press Button 1 alone: RESET timer starts, press again: stops, press again: resumes.</human-check>
  </acceptance_criteria>
  <done>Full Phase 2 button UX is wired. All four requirements BTN-04/05/06/07 are active. Project builds and behaves correctly on hardware.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ButtonHandler → main.cpp | isArmed(), startStopPressed(), stopResetPressed() return values; all hardware-sourced, already debounced by Bounce2 |
| main.cpp → TimerDisplay | setArmed(bool) call; value is boolean from ButtonHandler, no external input |
| main.cpp → emitTimerControl | All action string arguments are compile-time literals ("start", "stop", "reset") |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | timerDisplay.update() called before setArmed() | mitigate | loop() call order enforced: setArmed() immediately after poll(), update() after all button routing — wrong order would render one stale frame on arm transition |
| T-02-05 | Tampering | Blink state leak on disarm | mitigate | setArmed(false) resets _blink_state=true on disarm transition — display resumes visible immediately (Pitfall 3) |
| T-02-06 | Denial of Service | Armed display permanently stuck if _armed never cleared | accept | _armed is only ever cleared by _b2.rose() inside ButtonHandler::poll(); physical button release is the only path — no software path to get stuck |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages; platformio.ini unchanged |
</threat_model>

<verification>
After both tasks complete:
- `pio run` exits 0
- `grep -c "setArmed" include/TimerDisplay.h` returns 1
- `grep -c "setArmed" src/TimerDisplay.cpp` returns at minimum 1 (definition)
- `grep -c "emitTimerControl" src/main.cpp` returns at minimum 3 (stop+reset in chord block, plus start or stop in toggle block)
- `grep -c "setArmed(buttonHandler.isArmed())" src/main.cpp` returns 1
- Phase 2 success criteria from ROADMAP.md verified:
  1. Button 1 alone toggles running/stopped and never resets
  2. Button 2 held ≥400ms shows yellow blinking "STOP?"; releasing disarms with no state change
  3. Button 1 pressed while armed → stop and reset regardless of prior state
</verification>

<success_criteria>
All four Phase 2 requirements (BTN-04, BTN-05, BTN-06, BTN-07) are fully implemented and wired. The armed display overlay is functional. Button event routing through emitTimerControl() is complete. Project builds with `pio run` and the three ROADMAP success criteria for Phase 2 are met.
</success_criteria>

<output>
Create `.planning/phases/02-button-ux/02-02-SUMMARY.md` when done.
</output>
