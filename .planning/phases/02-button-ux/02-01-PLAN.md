---
phase: "02-button-ux"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - include/ButtonHandler.h
  - src/ButtonHandler.cpp
  - include/WebSocketClient.h
  - src/WebSocketClient.cpp
autonomous: true
requirements:
  - BTN-04
  - BTN-05
  - BTN-06

must_haves:
  truths:
    - "ButtonHandler exposes isArmed() (persistent), startStopPressed() (one-shot), stopResetPressed() (one-shot)"
    - "Armed state is entered when Button 2 has been held LOW for >=400ms and is still held"
    - "Armed state is cleared when Button 2 rises (rose()); releasing Button 2 alone causes no timer action"
    - "Button 1 fell while disarmed sets _startStopPending=true; while armed sets _stopResetPending=true"
    - "One-shot flags are reset to false at the top of every poll() call — before any evaluation"
    - "Arm check (set _armed=true) evaluates before Button 1 routing inside poll() — chord edge case safe"
    - "Phase 1 DEBUG_PRINTLN BTN1/BTN2 fell diagnostics are removed from poll() per D-12"
    - "WebSocketClient declares and implements emitTimerControl(const char* action)"
    - "emitTimerControl dispatches to _timer->start() / _timer->stop() / _timer->reset() via strcmp"
    - "pio run completes with 0 errors after these changes"
  artifacts:
    - path: "include/ButtonHandler.h"
      provides: "ButtonHandler class declaration with armed state machine API"
      exports: ["isArmed()", "startStopPressed()", "stopResetPressed()"]
    - path: "src/ButtonHandler.cpp"
      provides: "Armed state machine implementation"
      contains: "_b2.currentDuration() >= 400"
    - path: "include/WebSocketClient.h"
      provides: "emitTimerControl declaration"
      contains: "void emitTimerControl(const char* action)"
    - path: "src/WebSocketClient.cpp"
      provides: "emitTimerControl stub dispatching to _timer methods"
      contains: "strcmp(action, \"start\")"
  key_links:
    - from: "src/ButtonHandler.cpp poll()"
      to: "_startStopPending / _stopResetPending flags"
      via: "bool flags set inside poll(), read by main.cpp accessors"
      pattern: "_startStopPending = (false|true)"
    - from: "src/WebSocketClient.cpp emitTimerControl()"
      to: "_timer->start() / _timer->stop() / _timer->reset()"
      via: "strcmp dispatch on action string"
      pattern: "strcmp\\(action"
---

<objective>
Implement the ButtonHandler armed state machine (BTN-04, BTN-05, BTN-06) and the WebSocketClient timer control stub (D-05).

ButtonHandler gains three public accessors — `isArmed()`, `startStopPressed()`, `stopResetPressed()` — backed by an internal state machine inside `poll()` that tracks Button 2 hold duration, armed state, and one-shot event flags. WebSocketClient gains `emitTimerControl(const char* action)`, the Phase 2 stub that calls `_timer` directly and serves as the Phase 3 seam.

Purpose: Establishes the event API that main.cpp will consume in Plan 02. All four BTN-04/05/06 hardware behaviors are encoded here. D-12 cleanup also happens here.

Output: Modified ButtonHandler.h/cpp and WebSocketClient.h/cpp. Project must still build cleanly (`pio run`).
</objective>

<execution_context>
@/Users/themightypong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/themightypong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-button-ux/02-CONTEXT.md
@.planning/phases/02-button-ux/02-RESEARCH.md
@.planning/phases/02-button-ux/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ButtonHandler state machine — headers and implementation (D-01 through D-04, D-12, BTN-04, BTN-05, BTN-06)</name>
  <files>include/ButtonHandler.h, src/ButtonHandler.cpp</files>
  <read_first>
    - include/ButtonHandler.h — current class declaration (Phase 1 base to extend)
    - src/ButtonHandler.cpp — current poll() implementation with Phase 1 diagnostics to replace
    - .planning/phases/02-button-ux/02-PATTERNS.md §ButtonHandler.h and §ButtonHandler.cpp — exact code patterns, member names, constructor init list, accessor one-liners
    - .planning/phases/02-button-ux/02-RESEARCH.md §Pattern 1 — armed state machine poll() pattern with ordered evaluation
  </read_first>
  <action>
    Extend include/ButtonHandler.h:
    - After `void poll();`, add three public method declarations using `/// @brief` doxygen style (per D-01, D-02):
      `bool isArmed() const;` — persistent state
      `bool startStopPressed() const;` — one-shot per poll cycle, fires when Button 1 fell while disarmed
      `bool stopResetPressed() const;` — one-shot per poll cycle, fires when Button 1 fell while armed (D-03)
    - After `Bounce _b2;` in private section, add three private members:
      `bool _armed;`, `bool _startStopPending;`, `bool _stopResetPending;`

    Rewrite src/ButtonHandler.cpp:
    - Constructor initializer list: extend existing `: _pin1(pin1), _pin2(pin2)` to add `, _armed(false), _startStopPending(false), _stopResetPending(false)` before `{}`.
    - Keep the `#define DEBUG_BUTTONHANDLER` macro block at the top; set flag to `false` and update the diagnostic messages to Phase 2 state transitions: `"ARMED"`, `"DISARMED"`, `"START_STOP"`, `"STOP_RESET"` (per D-12 — remove the Phase 1 `BTN1 fell` / `BTN2 fell` output).
    - Replace the entire body of `poll()` with the ordered state machine (per D-04 and Pitfall 4 — arm evaluation MUST precede Button 1 routing):
      1. Call `_b1.update()` then `_b2.update()`.
      2. Reset one-shot flags at the very top: `_startStopPending = false; _stopResetPending = false;` (prevents double-fire per Pitfall 2).
      3. Arm check: `if (_b2.read() == LOW && _b2.currentDuration() >= 400) { _armed = true; }` — sets armed once threshold crossed while still held.
      4. Disarm on release: `if (_b2.rose()) { _armed = false; }` — releasing Button 2 always disarms (BTN-05: no timer action on release).
      5. Button 1 routing (AFTER arm state is settled): `if (_b1.fell()) { if (_armed) { _stopResetPending = true; } else { _startStopPending = true; } }` (per D-03).
      6. Optional DEBUG_PRINTLN calls guarded by `DEBUG_BUTTONHANDLER`: `"ARMED"` on arm transition, `"DISARMED"` on disarm, `"STOP_RESET"` when `_stopResetPending` set, `"START_STOP"` when `_startStopPending` set.
    - Add three const accessor implementations as one-liners after the existing `begin()` implementation:
      `bool ButtonHandler::isArmed() const { return _armed; }`
      `bool ButtonHandler::startStopPressed() const { return _startStopPending; }`
      `bool ButtonHandler::stopResetPressed() const { return _stopResetPending; }`
  </action>
  <verify>
    <automated>cd /Users/themightypong/Documents/PythonCode/Arena-Timer-Firmware-Standalone && pio run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - include/ButtonHandler.h public section contains `bool isArmed() const;`, `bool startStopPressed() const;`, `bool stopResetPressed() const;`
    - include/ButtonHandler.h private section contains `bool _armed;`, `bool _startStopPending;`, `bool _stopResetPending;`
    - src/ButtonHandler.cpp constructor initializer list contains `_armed(false)`, `_startStopPending(false)`, `_stopResetPending(false)`
    - src/ButtonHandler.cpp poll() body contains `_startStopPending = false;` and `_stopResetPending = false;` before any conditional
    - src/ButtonHandler.cpp poll() contains `_b2.currentDuration() >= 400` for arm detection
    - src/ButtonHandler.cpp poll() contains `_b2.rose()` for disarm detection
    - src/ButtonHandler.cpp poll() contains `_b1.fell()` with `_armed` branching
    - src/ButtonHandler.cpp does NOT contain `BTN1 fell` or `BTN2 fell` (Phase 1 diagnostics removed per D-12)
    - `pio run` exits 0
  </acceptance_criteria>
  <done>ButtonHandler exposes the full armed-state event API. Phase 1 diagnostics removed. Project builds.</done>
</task>

<task type="auto">
  <name>Task 2: WebSocketClient emitTimerControl stub (D-05, D-07)</name>
  <files>include/WebSocketClient.h, src/WebSocketClient.cpp</files>
  <read_first>
    - include/WebSocketClient.h — current public section structure (uses #ifndef guard, not #pragma once; keep as-is)
    - src/WebSocketClient.cpp — handleTimerUpdate() strcmp dispatch pattern (lines ~432–456); add new method at end of file
    - .planning/phases/02-button-ux/02-PATTERNS.md §WebSocketClient.h and §WebSocketClient.cpp — exact insertion point and implementation pattern
    - .planning/phases/02-button-ux/02-RESEARCH.md §Pattern 3 — emitTimerControl stub
  </read_first>
  <action>
    Modify include/WebSocketClient.h:
    - In the public section, after the `// Status` comment block (containing `getStatus()` and `getServerUrl()`), add a new comment group and declaration:
      `// Timer control (Phase 2: local stub; Phase 3: emits Socket.IO then calls local)`
      `void emitTimerControl(const char* action);`
    - Do not add any new private members — `_timer` pointer already exists.

    Modify src/WebSocketClient.cpp:
    - At the end of the file, add the `emitTimerControl()` implementation using the same `strcmp` dispatch pattern as `handleTimerUpdate()`:
      - `if (strcmp(action, "start") == 0)` → `DEBUG_PRINTLN("emitTimerControl: start"); _timer->start();`
      - `else if (strcmp(action, "stop") == 0)` → `DEBUG_PRINTLN("emitTimerControl: stop"); _timer->stop();`
      - `else if (strcmp(action, "reset") == 0)` → `DEBUG_PRINTLN("emitTimerControl: reset"); _timer->reset();`
    - Use the same `DEBUG_PRINTLN` macro already in the file. Action strings are `"start"`, `"stop"`, `"reset"` per D-07 — these match FightTimer's Socket.IO vocabulary exactly.
    - Note: `reset()` in this stub does NOT set duration from JSON (unlike the network path). The timer retains its current duration on reset — correct for chord-reset UX.
  </action>
  <verify>
    <automated>cd /Users/themightypong/Documents/PythonCode/Arena-Timer-Firmware-Standalone && pio run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - include/WebSocketClient.h public section contains `void emitTimerControl(const char* action);`
    - src/WebSocketClient.cpp contains `void WebSocketClient::emitTimerControl(const char* action)`
    - src/WebSocketClient.cpp emitTimerControl body contains `strcmp(action, "start")`, `strcmp(action, "stop")`, `strcmp(action, "reset")`
    - src/WebSocketClient.cpp emitTimerControl calls `_timer->start()`, `_timer->stop()`, `_timer->reset()` in respective branches
    - `pio run` exits 0
  </acceptance_criteria>
  <done>WebSocketClient has the emitTimerControl stub. Project builds. Phase 3 can replace the body with network emit prepended to the same local calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Button hardware → ButtonHandler | Active-low GPIO signals processed by Bounce2; only source of input in Phase 2 |
| main.cpp → emitTimerControl | Hardcoded literal string callers only; no external input reaches this method in Phase 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | ButtonHandler::poll() one-shot flags | mitigate | Clear `_startStopPending` and `_stopResetPending` at top of `poll()` before any evaluation — prevents double-fire if accessor called multiple times per cycle |
| T-02-02 | Tampering | Chord timing: arm check order in poll() | mitigate | Arm evaluation (`_b2.currentDuration() >= 400`) placed before `_b1.fell()` routing — prevents first chord at exactly 400ms from misfiring as start/stop |
| T-02-03 | Tampering | emitTimerControl null action pointer | accept | All callers in Phase 2 pass compile-time string literals; `wsClient` null-guarded in main.cpp; null dereference risk is zero in Phase 2 scope |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages in Phase 2; platformio.ini unchanged; no install tasks |
</threat_model>

<verification>
After both tasks complete:
- `pio run` exits 0
- `grep -c "BTN1 fell\|BTN2 fell" src/ButtonHandler.cpp` returns 0 (Phase 1 diagnostics removed)
- `grep -c "isArmed\|startStopPressed\|stopResetPressed" include/ButtonHandler.h` returns 3
- `grep -c "emitTimerControl" include/WebSocketClient.h` returns 1
- `grep -c "emitTimerControl" src/WebSocketClient.cpp` returns 1 (definition only; calls added in Plan 02)
</verification>

<success_criteria>
ButtonHandler exposes a clean three-method event API backed by an internal state machine that owns armed state (D-04). Phase 1 diagnostics removed (D-12). WebSocketClient has `emitTimerControl()` stub ready for Phase 3 seam (D-05). Project builds without errors. Plan 02 can consume these contracts without clarification.
</success_criteria>

<output>
Create `.planning/phases/02-button-ux/02-01-SUMMARY.md` when done.
</output>
